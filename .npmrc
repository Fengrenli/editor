# 缓解 monorepo 下 npm 解析 peer 依赖时的内部报错（如 Cannot read properties of null (reading 'matches')）
legacy-peer-deps=true

# 从 bun 改 npm 时务必先 npm run clean:node_modules，否则 workspace 里可能残留 tsc.bunx / tsc.exe，导致 tsc 指向不存在的 packages/.../typescript
